// Given two ll, determine if the two lists intersect.
// Return the intersecting node.
// Note that the intersection is defined based on reference, not value.
// That is, if the kth node of the first LL is the exact same node (by reference) as the jth node of the second LL, then they are intersecting.

// 3->1->5->9->7->2->1
// 4->6->7->2->1

/*


*/
const LinkedList = require("./utils/SinglyLL");

function intersection(list1, list2) {

}


intersection()

const intersectLL = new LinkedList();
intersectLL.addArray([7,2,1])

const test1 = new LinkedList();
test1.addArray([3, 1, 5, 9]);
test1.addNode(intersectLL.head)

const test2 = new LinkedList();
test2.addArray([4,6]);
test2.addNode(intersectLL.head)

console.log(test1.tail, test2.tail)